# swc-plugin-dev-expressions
Wraps calls to `warning()` in an `if ('production' !== node.env.NODE_ENV) {...}` 

To test it, try installing rustup (https://rustup.rs/#), then `cargo test` inside the root of the repo :) 
